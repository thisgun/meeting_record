# STT 엔진: faster-whisper 직접 사용 (Python 3.9~3.14 지원).
# (이전 WhisperX는 ctranslate2==4.4.0을 고정해 Python 3.13+ wheel이 없어 설치 불가였음)
faster-whisper>=1.1.0

# 고정밀 화자 분리(선택): HuggingFace 토큰 필요. 의존성이 무겁고 최신 Python에서는
# 미해결될 수 있어 자동 설치를 3.12 이하로 제한한다. 3.13+에서는 로컬(speechbrain)
# 화자 분리가 자동 사용되며, 필요하면 'pip install pyannote.audio'로 직접 설치 가능.
pyannote.audio>=3.1.0; python_version < "3.13"

# PyTorch — 여기서는 CPU 빌드가 설치됩니다(모든 환경에서 동작, GPU 불필요).
# NVIDIA GPU 가속은 '선택 사항'이며, 설치 후 README의 "GPU(CUDA) 가속" 절에 따라
# CUDA 빌드로 교체하세요. (환경별 CUDA 버전이 달라 requirements에 고정하지 않습니다.)
torch>=2.0.0
torchaudio>=2.0.0

# 로컬 화자 분리 (HF 토큰 없을 때 fallback)
speechbrain>=1.0.0
scikit-learn>=1.3.0
soundfile>=0.12.0

ollama>=0.3.0
requests>=2.31.0
python-dotenv>=1.0.0
pydub>=0.25.1
psutil>=5.9.0

# RAG 챗봇 (ask.py / qa_watcher.py) — 벡터 검색용. 임베딩 모델은 'ollama pull bge-m3'.
numpy>=1.24.0

# UI / export / analysis
streamlit>=1.50.0
python-docx>=1.0.0
watchdog>=4.0.0
huggingface-hub>=0.23.0

# 선택: 한국어 형태소 분석 정확도 향상. 필요할 때 별도 설치:
# pip install "kiwipiepy>=0.20.0"
